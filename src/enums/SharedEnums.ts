export enum GameState {
    LOBBY,
    GAMEPLAY
}

export enum Suit {
    SPADES = "Spades",
    DIAMONDS = "Diamonds",
    CLUBS = "Clubs",
    HEARTS = "Hearts"
}

export enum DECK_TYPES {
    STANDARD,
    EMPTY
}